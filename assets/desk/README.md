# Desk scene

- `studio-v6.webp`: final production image, lossless WebP, 1672 × 941.
- `studio-v6.png`: final master, retained for future edits.
- `generation-prompt-v6.txt`: exact prompt used with the built-in ImageGen tool.

The generator returned 1672 × 941 despite requesting 4K. The image is not upscaled. Older iterations and generation references have been removed.

Book titles, portrait, screens, publications and journal content are rendered separately by `desk/js/scene.js`. Each book title follows its own spine. Academic View is unchanged. See `desk/README.md` for the code structure.